[package]
name = "teiu-cobol"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Analisador léxico e sintático da seção de declarações de COBOL"

# Dependências justificadas na decisão D6 de docs/especificacao.md.
[dependencies]
clap = { version = "4.6", features = ["derive"] }
logos = "0.16"
