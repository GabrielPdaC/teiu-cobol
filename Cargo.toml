[package]
name = "teiu-cobol"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Analisador léxico e sintático da seção de declarações de COBOL"

# Sem dependências externas (decisão D6 em docs/especificacao.md).
[dependencies]
